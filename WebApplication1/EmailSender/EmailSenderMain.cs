using System.Net.Mail;
using System.Text;

namespace WebApplication1.EmailSender
{
    public class EmailSenderMain
    {
        /*
        MailAddress fromAddress = new MailAddress("ben@contoso.com");
        MailAddress toAddress = new MailAddress("Jane@contoso.com");

        SmtpClient client = new SmtpClient();
        client.Port = 25;
        client.Host = "smtp.gmail.com";
        client.EnableSsl = true;
        client.Timeout = 10000;
        client.DeliveryMethod = SmtpDeliveryMethod.Network;
        client.UseDefaultCredentials = false;
        client.Credentials = new System.Net.NetworkCredential("user@gmail.com","password");

        static MailMessage mm = new MailMessage(fromAddress, toAddress);
        mm.Subject="Your Flight Confirmation";
        mm.Body="Thank you for choosing Iowa Air";
        mm.DeliveryNotificationOptions = DeliveryNotificationOptions.OnFailure;

        client.Send(mm);
        */
    }
}


